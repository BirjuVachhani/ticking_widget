name: example
description: "An example for ticking_widget package."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  ticking_widget:
    path: ../
  intl: ">=0.18.0 <1.0.0"
  dart_style: ^2.3.6
  flutter_highlight: ^0.7.0
  syntax_highlight: ^0.4.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true